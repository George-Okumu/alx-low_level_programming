#include "holberton.h"
#include <stdio.h>
/**
 * add - adds two integers
 * @n: for first number
 * @num: for second number
 * Return: the result of the two numbers
 */
int add(int n, int num)
{
return (n + num);
}
