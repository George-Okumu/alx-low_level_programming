#include "holberton.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: integer to be tested
 * Return: positive only
 */
int _abs(int n)
{
if (n <= 0)
{
n = -n;
return (n);
}
else
{
return (n);
}
}
